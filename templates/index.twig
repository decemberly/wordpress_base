{% extends "base.twig" %}

{% block content %}
	<h2 class="c-heading-b">{{foo}}</h2>
	<p>{{qux}}</p>
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}
{% endblock %}
